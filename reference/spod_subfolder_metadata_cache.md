# Get metadata cache subfolder name

Change subfolder name in the code of this function for metadata cache
here to apply globally, as all functions in the package should use this
function to get the metadata cache path.

## Usage

``` r
spod_subfolder_metadata_cache()
```

## Value

A `character` string with the subfolder name for the raw data cache.
