# Get valid dates from the GraphQL API

Get valid dates from the GraphQL API

## Usage

``` r
spod_graphql_valid_dates()
```

## Value

A `Date` vector of dates that are valid to request data with
[`spod_quick_get_od()`](https://rOpenSpain.github.io/spanishoddata/reference/spod_quick_get_od.md).
