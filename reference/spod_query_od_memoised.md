# Cache the spod_query_od_raw function to avoid repeated requests

Cache the spod_query_od_raw function to avoid repeated requests

## Usage

``` r
spod_query_od_memoised(
  date_fmt,
  graphql_distances,
  id_origin,
  id_destination,
  min_trips,
  graphql_query
)
```
