# Get the path to the V1 metadata RDS file

Get the path to the V1 metadata RDS file

## Usage

``` r
spod_get_v1_meta_path()
```

## Value

A `character` string with the path to the RDS file, or `""` if not
found.
